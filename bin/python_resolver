#!/usr/bin/env python
import sys
import os

if len(sys.argv) < 2:
    exit(2)

relative = sys.argv[1]

def absolute_path(base):
    return base + relative

paths = filter(os.path.exists, map(absolute_path, sys.path))

if len(paths) > 0:
    print paths[0]
else:
    exit(1)
